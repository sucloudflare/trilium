const { contextBridge, ipcRenderer } = require('electron');

contextBridge.exposeInMainWorld('triliumAPI', {
    onDeepLink: (callback) => ipcRenderer.on('deep-link', (event, url) => callback(url))
});
